# Library Documentation

Core utilities, services, database access, schemas, and stores live under `src/lib/`.

**Architecture and conventions**: [`AGENTS.md`](../../AGENTS.md)  
**Product spec and roadmap**: [`spec.md`](../../spec.md)

Do not rely on outdated references in this folder to `actions.ts`, `db.ts`, or monolithic `schemas.ts` — those paths were refactored into `recipe-generation.server.ts`, `db/`, and `schemas/`.
